
<nav class="top-bar">
  <ul class="title-area">
    <li class="name">
        <h1><a href=#><%= page_title %></a></h1>
    </li>
  </ul>
  <section class="top-bar-section">
    <% unless items.empty? %>
        <ul class="right">
        <% items.each do |item| %>
            <%= render :partial => "menu_item", :object => item %>
        <% end %>
        </ul>
    <% end %>
  </section>
</nav>